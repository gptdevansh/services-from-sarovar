This is the Next.js `public` directory for the frontend.

Place static assets (images, icons, favicons, robots.txt, manifest, etc.) here. These files will be served at the root URL (e.g., `/image.png`).
